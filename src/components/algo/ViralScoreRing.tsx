/**
 * Réexport : une seule implémentation (`computeViralScore`, a11y `meter`, couleurs canoniques).
 * Imports legacy `@/components/algo/ViralScoreRing` restent valides ; préférer `@/components/ui/ViralScoreRing` pour le code neuf.
 */
export { ViralScoreRing } from "@/components/ui/ViralScoreRing";
